{
  "rank_ssh_attackers": {
    "title": "Rank SSH Attackers Input",
    "type": "object",
    "properties": {
      "limit": {
        "title": "Result Limit",
        "description": "Maximum number of ranked source IPs to return.",
        "type": "integer",
        "required": "false",
        "default": 5,
        "minimum": 1,
        "maximum": 25,
        "examples": [3, 5, 10],
        "validation_message": "Limit must be an integer between 1 and 25."
      }
    }
  },
  "get_ssh_source_profile": {
    "title": "SSH Source Profile Input",
    "type": "object",
    "properties": {
      "ip": {
        "title": "Source IPv4 Address",
        "description": "IPv4 source address to investigate, normally copied from rank_ssh_attackers.",
        "type": "string",
        "required": "true",
        "pattern": "^(?:[0-9]{1,3}\\.){3}[0-9]{1,3}$",
        "examples": ["87.194.216.51"],
        "validation_message": "IP must be a valid IPv4 address."
      }
    }
  },
  "list_ssh_source_targets": {
    "title": "SSH Source Targets Input",
    "type": "object",
    "properties": {
      "ip": {
        "title": "Source IPv4 Address",
        "description": "IPv4 source address to investigate, normally copied from rank_ssh_attackers.",
        "type": "string",
        "required": "true",
        "pattern": "^(?:[0-9]{1,3}\\.){3}[0-9]{1,3}$",
        "examples": ["87.194.216.51"],
        "validation_message": "IP must be a valid IPv4 address."
      },
      "limit": {
        "title": "Result Limit",
        "description": "Maximum host rows and username rows to return.",
        "type": "integer",
        "required": "false",
        "default": 10,
        "minimum": 1,
        "maximum": 25,
        "examples": [5, 10],
        "validation_message": "Limit must be an integer between 1 and 25."
      }
    }
  }
}
